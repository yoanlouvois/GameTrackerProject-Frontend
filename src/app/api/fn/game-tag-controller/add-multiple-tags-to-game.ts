/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { GameTagDto } from '../../models/game-tag-dto';
import { Tag } from '../../models/tag';

export interface AddMultipleTagsToGame$Params {
  gameId: number;
  tags: Array<Tag>;
}

export function addMultipleTagsToGame(http: HttpClient, rootUrl: string, params: AddMultipleTagsToGame$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<GameTagDto>>> {
  const rb = new RequestBuilder(rootUrl, addMultipleTagsToGame.PATH, 'post');
  if (params) {
    rb.path('gameId', params.gameId, {});
    rb.query('tags', params.tags, {});
  }

  return http.request(
    rb.build({ responseType: 'blob', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<GameTagDto>>;
    })
  );
}

addMultipleTagsToGame.PATH = '/gametracker/v1/game/{gameId}/tags';
