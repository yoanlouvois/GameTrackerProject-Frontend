/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Pageable } from '../../models/pageable';
import { PageMessageDto } from '../../models/page-message-dto';

export interface GetMessagesByReceiver$Params {
  receiverId: number;
  pageable: Pageable;
}

export function getMessagesByReceiver(http: HttpClient, rootUrl: string, params: GetMessagesByReceiver$Params, context?: HttpContext): Observable<StrictHttpResponse<PageMessageDto>> {
  const rb = new RequestBuilder(rootUrl, getMessagesByReceiver.PATH, 'get');
  if (params) {
    rb.path('receiverId', params.receiverId, {});
    rb.query('pageable', params.pageable, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageMessageDto>;
    })
  );
}

getMessagesByReceiver.PATH = '/gametracker/v1/message/receiver/{receiverId}';
