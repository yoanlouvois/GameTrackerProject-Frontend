/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { UserAchievementDto } from '../../models/user-achievement-dto';

export interface GetRecentUnlocks$Params {
  userId: number;
  days: number;
}

export function getRecentUnlocks(http: HttpClient, rootUrl: string, params: GetRecentUnlocks$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<UserAchievementDto>>> {
  const rb = new RequestBuilder(rootUrl, getRecentUnlocks.PATH, 'get');
  if (params) {
    rb.path('userId', params.userId, {});
    rb.path('days', params.days, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<UserAchievementDto>>;
    })
  );
}

getRecentUnlocks.PATH = '/gametracker/v1/userAchievements/recent/{userId}/{days}';
