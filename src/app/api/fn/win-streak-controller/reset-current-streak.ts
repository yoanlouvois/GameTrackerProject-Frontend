/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { WinStreakDto } from '../../models/win-streak-dto';

export interface ResetCurrentStreak$Params {
  userId: number;
  gameId: number;
}

export function resetCurrentStreak(http: HttpClient, rootUrl: string, params: ResetCurrentStreak$Params, context?: HttpContext): Observable<StrictHttpResponse<WinStreakDto>> {
  const rb = new RequestBuilder(rootUrl, resetCurrentStreak.PATH, 'put');
  if (params) {
    rb.path('userId', params.userId, {});
    rb.path('gameId', params.gameId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<WinStreakDto>;
    })
  );
}

resetCurrentStreak.PATH = '/gametracker/v1/streaks/reset/{userId}/{gameId}';
