/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { GameCommentDto } from '../../models/game-comment-dto';

export interface UpdateCommentContent$Params {
  commentId: number;
      body: string
}

export function updateCommentContent(http: HttpClient, rootUrl: string, params: UpdateCommentContent$Params, context?: HttpContext): Observable<StrictHttpResponse<GameCommentDto>> {
  const rb = new RequestBuilder(rootUrl, updateCommentContent.PATH, 'put');
  if (params) {
    rb.path('commentId', params.commentId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<GameCommentDto>;
    })
  );
}

updateCommentContent.PATH = '/gametracker/v1/comments/update/{commentId}';
