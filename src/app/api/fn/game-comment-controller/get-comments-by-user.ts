/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Pageable } from '../../models/pageable';
import { PageGameCommentDto } from '../../models/page-game-comment-dto';

export interface GetCommentsByUser$Params {
  userId: number;
  pageable: Pageable;
}

export function getCommentsByUser(http: HttpClient, rootUrl: string, params: GetCommentsByUser$Params, context?: HttpContext): Observable<StrictHttpResponse<PageGameCommentDto>> {
  const rb = new RequestBuilder(rootUrl, getCommentsByUser.PATH, 'get');
  if (params) {
    rb.path('userId', params.userId, {});
    rb.query('pageable', params.pageable, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageGameCommentDto>;
    })
  );
}

getCommentsByUser.PATH = '/gametracker/v1/comments/user/{userId}';
