/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { GameRatingDto } from '../../models/game-rating-dto';

export interface GetRatingById$Params {
  ratingId: number;
}

export function getRatingById(http: HttpClient, rootUrl: string, params: GetRatingById$Params, context?: HttpContext): Observable<StrictHttpResponse<GameRatingDto>> {
  const rb = new RequestBuilder(rootUrl, getRatingById.PATH, 'get');
  if (params) {
    rb.path('ratingId', params.ratingId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<GameRatingDto>;
    })
  );
}

getRatingById.PATH = '/gametracker/v1/rating/{ratingId}';
