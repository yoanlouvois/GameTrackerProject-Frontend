/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Pageable } from '../../models/pageable';
import { PageGameRatingDto } from '../../models/page-game-rating-dto';

export interface SearchRatings$Params {
  pageable: Pageable;
      body: string
}

export function searchRatings(http: HttpClient, rootUrl: string, params: SearchRatings$Params, context?: HttpContext): Observable<StrictHttpResponse<PageGameRatingDto>> {
  const rb = new RequestBuilder(rootUrl, searchRatings.PATH, 'get');
  if (params) {
    rb.query('pageable', params.pageable, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageGameRatingDto>;
    })
  );
}

searchRatings.PATH = '/gametracker/v1/rating/search';
