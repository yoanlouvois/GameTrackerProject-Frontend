/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { GameProgressDto } from '../../models/game-progress-dto';

export interface GetAllUserProgress$Params {
  userId: number;
}

export function getAllUserProgress(http: HttpClient, rootUrl: string, params: GetAllUserProgress$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<GameProgressDto>>> {
  const rb = new RequestBuilder(rootUrl, getAllUserProgress.PATH, 'get');
  if (params) {
    rb.path('userId', params.userId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<GameProgressDto>>;
    })
  );
}

getAllUserProgress.PATH = '/gametracker/v1/progress/user/{userId}/all';
