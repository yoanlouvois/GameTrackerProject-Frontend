/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { GameProgressDto } from '../../models/game-progress-dto';

export interface UpdateGameplaySession$Params {
  progressId: number;
  scoreDelta: number;
  timeDelta: number;
}

export function updateGameplaySession(http: HttpClient, rootUrl: string, params: UpdateGameplaySession$Params, context?: HttpContext): Observable<StrictHttpResponse<GameProgressDto>> {
  const rb = new RequestBuilder(rootUrl, updateGameplaySession.PATH, 'put');
  if (params) {
    rb.path('progressId', params.progressId, {});
    rb.query('scoreDelta', params.scoreDelta, {});
    rb.query('timeDelta', params.timeDelta, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<GameProgressDto>;
    })
  );
}

updateGameplaySession.PATH = '/gametracker/v1/progress/{progressId}/update';
