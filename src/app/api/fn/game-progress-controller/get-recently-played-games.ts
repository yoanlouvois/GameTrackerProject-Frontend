/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { GameProgressDto } from '../../models/game-progress-dto';

export interface GetRecentlyPlayedGames$Params {
  userId: number;
  hours: number;
}

export function getRecentlyPlayedGames(http: HttpClient, rootUrl: string, params: GetRecentlyPlayedGames$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<GameProgressDto>>> {
  const rb = new RequestBuilder(rootUrl, getRecentlyPlayedGames.PATH, 'get');
  if (params) {
    rb.path('userId', params.userId, {});
    rb.query('hours', params.hours, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<GameProgressDto>>;
    })
  );
}

getRecentlyPlayedGames.PATH = '/gametracker/v1/progress/user/{userId}/recent';
