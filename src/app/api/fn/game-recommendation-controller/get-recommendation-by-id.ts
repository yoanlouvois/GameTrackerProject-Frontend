/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { GameRecommendationDto } from '../../models/game-recommendation-dto';

export interface GetRecommendationById$Params {
  recommendationId: number;
}

export function getRecommendationById(http: HttpClient, rootUrl: string, params: GetRecommendationById$Params, context?: HttpContext): Observable<StrictHttpResponse<GameRecommendationDto>> {
  const rb = new RequestBuilder(rootUrl, getRecommendationById.PATH, 'get');
  if (params) {
    rb.path('recommendationId', params.recommendationId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<GameRecommendationDto>;
    })
  );
}

getRecommendationById.PATH = '/gametracker/v1/recommendation/{recommendationId}';
