/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Pageable } from '../../models/pageable';
import { PageGameRecommendationDto } from '../../models/page-game-recommendation-dto';

export interface SearchRecommendations$Params {
  pageable: Pageable;
      body: string
}

export function searchRecommendations(http: HttpClient, rootUrl: string, params: SearchRecommendations$Params, context?: HttpContext): Observable<StrictHttpResponse<PageGameRecommendationDto>> {
  const rb = new RequestBuilder(rootUrl, searchRecommendations.PATH, 'get');
  if (params) {
    rb.query('pageable', params.pageable, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageGameRecommendationDto>;
    })
  );
}

searchRecommendations.PATH = '/gametracker/v1/recommendation/search';
