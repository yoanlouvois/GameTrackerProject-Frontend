/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Pageable } from '../../models/pageable';
import { PageGameRecommendationDto } from '../../models/page-game-recommendation-dto';

export interface GetAllRecommendations$Params {
  pageable: Pageable;
}

export function getAllRecommendations(http: HttpClient, rootUrl: string, params: GetAllRecommendations$Params, context?: HttpContext): Observable<StrictHttpResponse<PageGameRecommendationDto>> {
  const rb = new RequestBuilder(rootUrl, getAllRecommendations.PATH, 'get');
  if (params) {
    rb.query('pageable', params.pageable, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageGameRecommendationDto>;
    })
  );
}

getAllRecommendations.PATH = '/gametracker/v1/recommendation/all';
