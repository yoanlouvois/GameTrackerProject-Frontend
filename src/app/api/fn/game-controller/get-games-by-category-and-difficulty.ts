/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Game } from '../../models/game';

export interface GetGamesByCategoryAndDifficulty$Params {
  category: 'ACTION' | 'ADVENTURE' | 'ARCADE' | 'BOARD' | 'CARD' | 'CASINO' | 'RPG' | 'STRATEGY';
  difficultyLevel: 'EASY' | 'MEDIUM' | 'HARD';
}

export function getGamesByCategoryAndDifficulty(http: HttpClient, rootUrl: string, params: GetGamesByCategoryAndDifficulty$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<Game>>> {
  const rb = new RequestBuilder(rootUrl, getGamesByCategoryAndDifficulty.PATH, 'get');
  if (params) {
    rb.query('category', params.category, {});
    rb.query('difficultyLevel', params.difficultyLevel, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<Game>>;
    })
  );
}

getGamesByCategoryAndDifficulty.PATH = '/gametracker/v1/game/category/difficulty';
