/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Pageable } from '../../models/pageable';
import { PageGameDto } from '../../models/page-game-dto';

export interface GetAllGames$Params {
  pageable: Pageable;
}

export function getAllGames(http: HttpClient, rootUrl: string, params: GetAllGames$Params, context?: HttpContext): Observable<StrictHttpResponse<PageGameDto>> {
  const rb = new RequestBuilder(rootUrl, getAllGames.PATH, 'get');
  if (params) {
    rb.query('pageable', params.pageable, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageGameDto>;
    })
  );
}

getAllGames.PATH = '/gametracker/v1/game/all';
